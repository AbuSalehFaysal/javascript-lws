// Statements - Examples
document.getElementById("demo").innerHTML = "Hello World!";

// Statements execute one after another

// Use semicolon after the end of a statement

// JS ignores multiple spaces

// Use spaces around operator(+,-,*,/) - Good Practice

// If a JS statement does not fit in one line, is is good practice to break it after an operator

// JS statements can be group together in code block, inside curly braces {...}