// JS Object Maps

// Map hold key value pair just like objects
// map can hold data types as key, whereas Object can hold string as key

const person = {
  name: "Abu Saleh Faysal",
  age: 25,
};

let pName = person["name"]; // name is string

// for map the order of the keys are important unlike objects
// maps has a size property, but object does not have any size property

// ways of creating map --- new Map ([10, 20, 30])

// map.sep to add key-value or edit-update key-value

// map.get(key) // return value
// map.delete(key) // delete targeted key-value

// map.clear() // to remove all the elements from map

// map.has // check the targeted element is there or not

// typeOf map // objects

// instanceOf map // map

// Object Vs Map // ***Interview***
