// JS Strings

// ""

// ' '

let lengthX = "Faysal";
console.log(lengthX.length); // return the length of the string.

let x = 'Abu Saleh "Faysal"'; // double quotes inside single quotes
let y = "It's a string"; // single quote inside double quotes

// \' or \" will act as an escape character and ignore the next char.

// \` == single quote
// \" == double quote

let vikings = 'We are the so called "Vikings" from the north!';

// == check the value

// === check the value and the type

// we cannot compare the two objects using == or ===, it will return false always

// six other escape character are valid in javascript. Such as /b, /n
