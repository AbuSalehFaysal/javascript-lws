// JS Iterables (array, strings, maps, NodeLists, Sets) --- basically it means we can run iteration using these(Order is important here).

// iterating means looping over a sequence of elements ***Interview***

// for iterables, we should use for of

// Set, in set only unique elements are stored.
