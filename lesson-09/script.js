// JS Operators

// assignment operator (=, +=, -=, *=, /=, %=, **=)

// arithmetic operators (+, -, *, /, %, ++, --, **)

// the + operator can also be used to concatenate strings. e.g. "hello" + "world" = "helloworld"

// adding a string and a number will return a string. e.g. "hello" + 1 = "hello1"

// comparison operators (==, ===, !=, !==, >, <, >=, <=, ?)

// logical operators (&&, ||, !)

// Type Operator

// typeof operator (returns the type of the operand)

// instanceof operator (returns true if the operand is an instance of the class)

// bitwise operators (<<, >>, >>>)