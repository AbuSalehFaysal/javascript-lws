// JS comparisons

// == (compare the values only)
// === (compare the values and datatypes)

// != (not equal value)
// !== (not equal value or datatype)

// > (greater than)
// < (less than)
// >= (greater than or equal)
// <= (less than or equal)

// Logical operator
// && (AND operator)
// || (OR operator)
// ! (Not operator)

// ternary operator
let age = 25;
let xyz = age >= 25 ? "Dead!" : "Not Yet!";
